function reportsPage() {
  const d=F.dashboard(state), entries=state.journal||[];
  const vatRows=entries.flatMap(j=>j.rows.map(r=>({...r,date:j.date,number:j.number}))).filter(r=>/^26/.test(r.account));
  const output=vatRows.filter(r=>!/^2641/.test(r.account)).reduce((n,r)=>n+(r.credit||0)-(r.debit||0),0);
  const input=vatRows.filter(r=>/^2641/.test(r.account)).reduce((n,r)=>n+(r.debit||0)-(r.credit||0),0);
  const rows=entries.slice(0,12).map(j=>`<tr><td>${j.date}</td><td><b>${escapeHtml(j.number)}</b></td><td>${escapeHtml(j.description)}</td><td class="number-cell">${money(j.rows.reduce((n,r)=>n+(r.debit||0),0))}</td></tr>`).join('');
  return workspaceChrome(`${heading('Rapporter','Ekonomisk överblick baserad på fakturor, betalningar och verifikationer.','<button class="button" data-action="download">Exportera bokföring</button>')}<section class="metrics report-metrics"><article class="metric"><span>Resultat hittills</span><div class="number">${money(d.revenue-d.expense)}</div><small>Intäkter ${money(d.revenue)} · Kostnader ${money(d.expense)}</small></article><article class="metric"><span>Kundfordringar</span><div class="number">${money(d.ar.open)}</div><small>${d.ar.count} poster</small></article><article class="metric"><span>Leverantörsskulder</span><div class="number">${money(d.ap.open)}</div><small>${d.ap.count} poster</small></article><article class="metric"><span>Bankhändelser att stämma av</span><div class="number">${state.bankTransactions.filter(t=>t.status==='Granska').length}</div><small>Se Granska & åtgärda</small></article></section><section class="report-grid"><article class="panel"><div class="panel-head"><div><h2>Åldersanalys kundfordringar</h2><p class="hint">Öppna belopp per förfallointervall.</p></div></div><div class="report-bars">${F.aging(d.customers).map(a=>`<div class="report-bar"><span>${a.label}</span><b>${money(a.value)}</b><i style="width:${d.ar.open?Math.min(100,a.value/d.ar.open*100):0}%"></i></div>`).join('')}</div></article><article class="panel"><div class="panel-head"><div><h2>Momsöversikt</h2><p class="hint">Baserad på bokförda momskonton.</p></div></div><div class="report-kpis"><div><span>Utgående moms</span><b>${money(output)}</b></div><div><span>Ingående moms</span><b>${money(input)}</b></div><div><span>Netto moms</span><b>${money(output-input)}</b></div></div></article></section><section class="panel"><div class="panel-head"><div><h2>Senaste verifikationer</h2><p class="hint">Alla belopp avrundas till hela kronor.</p></div></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Datum</th><th>Ver.</th><th>Beskrivning</th><th class="align-right">Debet totalt</th></tr></thead><tbody>${rows||'<tr><td colspan="4" class="empty">Inga verifikationer ännu.</td></tr>'}</tbody></table></div></section><section class="integration"><b>Databas & säkerhet</b><br>Plattformen sparar företagets register, reskontra, bankhändelser och verifikationer i den lokala ekonomidatabasen. Exportera bokföringen regelbundet och säkerhetskopiera lagringsmappen innan produktionsdrift.</section>`);
}
